<% provide(:title, 'Edit Interest') %>
<h1>Update
        <%= @interest.name %>
  </h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
        <%= link_to "Delete Interest", @interest, method: :delete,
                  data: { confirm: "You sure?" } %>
</div>
