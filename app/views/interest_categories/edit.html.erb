<% provide(:title, 'Edit Interest Category') %>
<h1>Update
        <%= @interest_category.name %>
  </h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
        <%= link_to "Delete Interest Category", @interest_category, method: :delete,
                  data: { confirm: "You sure?" } %>
</div>
